
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Simple rule: Allow read/write access to any authenticated user.
    // This is for rapid prototyping and should be secured before production.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
